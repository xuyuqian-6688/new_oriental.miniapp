// let host = 'http://neworiental.local.com'; // dev
let host = 'https://new-oriental.lmh5.com'
// let host = 'https://new-oriental-uat.vdoou.cn'; // uat
let baseURL = '/api/wx'

let config = {
  service: {
    host,
    baseURL
  }
};
 
module.exports = config;